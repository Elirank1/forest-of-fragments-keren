# Forest of Fragments MVP Overview

The prototype uses one self-contained run to establish tone, family profiles, guardians, destroyers, and extensibility. All game content is configured in data modules so future content additions stay incremental.
